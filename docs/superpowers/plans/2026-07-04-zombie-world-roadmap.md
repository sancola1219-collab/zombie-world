# 《殭屍世界：黑鴉市事件》實作路線圖

規格：`docs/superpowers/specs/2026-07-04-zombie-world-fps-design.md`（已核准）

整個遊戲拆成六個階段，**每階段一份獨立實作計畫、每階段結束都有可玩可驗證的成果**。完成一階段後才撰寫下一階段的詳細計畫（依當時實際程式碼狀態調整）。

| 階段 | 內容 | 可驗證成果 | 計畫文件 |
|---|---|---|---|
| 1 | 引擎基礎：固定時步迴圈、程序貼圖、資料驅動關卡生成、碰撞、輸入抽象、音效合成、存檔層、沙盒地圖 | 在兩房沙盒中第一人稱行走、開門、遇上鎖門，分頁隱藏不當機 | `2026-07-04-zombie-world-phase1-engine.md`（本次撰寫） |
| 2 | 戰鬥與物品：武器射擊（小刀/手槍/霰彈/麥格農）、殭屍與殭屍犬 AI、心電圖 HUD、8 格物品欄、草藥組合、打字機存檔整合 | 示範戰鬥房：打殭屍、撿彈藥、用草藥、打字機存讀檔 | 階段 1 完成後撰寫 |
| 3 | 第一章〈月影洋館〉完整：謎題狀態機、文件閱讀系統、樓層地圖、章節推進鏈、獵痕者、全部洋館房間與事件 | 第一章從玄關到纜車站可完整通關 | 階段 2 完成後撰寫 |
| 4 | 第二章〈黑鴉市警察局〉：街道序段、女神像謎題鏈、潛伏者、執行者（不死追跡 AI） | 第二章可完整通關，執行者追擊運作 | 階段 3 完成後撰寫 |
| 5 | 第三章〈晨星地下實驗室〉：接線謎題、化學調配、原體兩階段 Boss、火箭筒、自毀倒數、結局與評價 | 全遊戲三章通關＋S–D 評價 | 階段 4 完成後撰寫 |
| 6 | 手機觸控（雙搖桿＋按鈕）、效能降級、難度平衡、全流程驗收、發佈 GitHub Pages `…/zombie-world/` | 手機可通關；公開網址上線 | 階段 5 完成後撰寫 |

## 跨階段固定約定（所有計畫共用）

- 純 ES modules；Three.js 僅經 import map 載入 `three@0.170.0`（jsdelivr CDN），除此之外零外部請求
- **邏輯與渲染分離**：遊戲邏輯只跑在 `GameLoop` 固定時步（60Hz）內；rAF 只負責渲染；分頁隱藏時由 `setInterval` 後備驅動邏輯
- **可測性**：`js/game/*`、`js/engine/loop.js`、`js/engine/save.js`、`js/game/world.js` 等邏輯模組**不得 import three、不得碰 DOM**（事件接線集中在 `attach()` 方法與 `main.js`），全部可在 `node --test` 下同步測試
- 測試指令：`node --test tests/`；測試檔命名 `tests/<模組>.test.js`
- 瀏覽器驗證：`.claude/launch.json` 的 `zombie-world` 設定（`npx http-server -p 8123`），用 preview 工具檢查
- 所有 UI 與文案繁體中文；座標約定：x 向東、z 向南、y 向上；yaw=0 面向 −z，`rotation.order='YXZ'`
- 隨機一律用 `js/engine/rng.js` 的 `mulberry32(seed)`（可重現、可測）
- 每完成一個任務就 commit
